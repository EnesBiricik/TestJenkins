services:
     testapi:
       build:
         context: .
         dockerfile: Dockerfile
       ports:
         - "1000:1000"
       environment:
         - ASPNETCORE_ENVIRONMENT=Development
       container_name: c_testapi